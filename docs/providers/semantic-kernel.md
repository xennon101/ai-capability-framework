# Semantic Kernel

AICF supports Semantic Kernel-compatible hosts through MCP guidance and OpenAPI plugin
metadata export. It does not provide a native Semantic Kernel runtime.

Read:

- [Semantic Kernel compatibility](../semantic-kernel-runtime.md)

MCP is the recommended path when available. OpenAPI export is a descriptor for
host-owned executor routes.
