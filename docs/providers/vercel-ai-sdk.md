# Vercel AI SDK

The AI SDK provider subpath builds executor-backed tools for host-supplied
`generateText` and `streamText` functions.

Read:

- [Vercel AI SDK bridge](../ai-sdk-runtime.md)

AICF does not install model provider packages or call models by default.
