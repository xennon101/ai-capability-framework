# Fixtures

Trigger coverage and validation fixtures are added in S2.

Do not add fixture filenames or content that look like credentials, raw prompts,
provider payloads, traces, or private local files.
