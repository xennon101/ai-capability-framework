# Source Archive Rules

Use the repository source archive script when available. The archive should be based on tracked files, not a manual workspace copy.

Reject source archives that include private folders, generated outputs, dependency folders, nested archives, logs, traces, prompt dumps, provider transport captures, credential files, or local-only state.
