# Redacted Telemetry

Default telemetry should include IDs, hashes, counts, status, reasons, provider/model
metadata, latency, token or cost summaries, and safe outcome summaries.

Default telemetry should not include private prompts, provider transport bodies,
secrets, stack traces, account IDs, tenant IDs, or sensitive tool output.
