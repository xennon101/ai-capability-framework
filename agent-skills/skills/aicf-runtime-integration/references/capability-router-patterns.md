# Capability Router Patterns

- Filter disabled, deprecated, or restricted capabilities by default.
- Enforce permissions, tenant/account context, autonomy limits, risk ceilings,
  include/exclude lists, domains, and max count.
- Score with deterministic metadata and workflow context.
- Expose read and prepare operations to models; keep commit host-controlled.
- Return exclusion reasons for debugging without private diagnostics.
